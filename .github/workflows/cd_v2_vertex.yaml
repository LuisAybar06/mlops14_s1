name: Build & Deploy Cloud Run Job (Vertex Pipeline)

on:
  workflow_dispatch:

# on:
#   push:
#     branches:
#       - 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      PROJECT_ID: ${{ secrets.GOOGLE_PROJECT }}
      REGION: us-central1
      IMAGE_REPOSITORY: imagen
      IMAGE_NAME: vertex_pipeline


    steps:
    - name: Checkout code
      uses: actions/checkout@v3


    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

    - name: Setup gcloud
      uses: google-github-actions/setup-gcloud@v2
      with:
        project_id: ${{ secrets.GOOGLE_PROJECT }}
        export_default_credentials: true

    # Construir y publicar la imagen Docker en Artifact Registry
    - name: build and push the docker image 
      env:
        GOOGLE_PROJECT: ${{ secrets.GOOGLE_PROJECT }} 
      run: | 
        # Configurar Docker para autenticar con Artifact Registry
        gcloud auth configure-docker us-central1-docker.pkg.dev
        # Construir imagen
        docker build -t us-central1-docker.pkg.dev/$GOOGLE_PROJECT/$IMAGE_REPOSITORY/$IMAGE_NAME:latest ./despliegue_cloud_run
        # Publicar imagen Docker en Artifact Registry
        docker push us-central1-docker.pkg.dev/$GOOGLE_PROJECT/$IMAGE_REPOSITORY/$IMAGE_NAME:latest



    - name: Deploy Cloud Run Job
      env:
        GOOGLE_PROJECT: ${{ secrets.GOOGLE_PROJECT }} 
      run: |
        gcloud run jobs deploy run-pipeline-job \
          --image us-central1-docker.pkg.dev/$GOOGLE_PROJECT/$IMAGE_REPOSITORY/$IMAGE_NAME \
          --region $REGION \
          --service-account cloud-run-sa@$GOOGLE_PROJECT.iam.gserviceaccount.com \
          --set-env-vars PROJECT_ID=mlops-test-487414,LOCATION=us-central1,PIPELINE_PATH=gs://vertex_pipelines_mlops/demo/pipeline_bigquery.json,SERVICE_ACCOUNT=vertex-ia-procces@mlops-test-487414.iam.gserviceaccount.com,VALIDA_TABLE=mlops-test-487414.testsesion.census_by_age,INPUT_AGE=60 \
          --quiet
